import { styled } from '@mui/system';

export const Wrapper=styled('div')(({theme})=>({
  margin: '0 auto',
  width:'100%',
  maxWidth:'800px'
}))

export const StepperUI=styled('div')(({theme})=>({
   marginBottom:'16px'
  }))
